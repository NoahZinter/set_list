<h1>This Artist's Songs</h1>

<% @artist_songs.each do |song| %>
  <%= link_to song.title, "/songs/#{song.id}" %>
  <% end %>
  <p>Average Song Length for <%= @artist.name %>: <%= @artist.average_song_length %> </p>